{
  "name": "stackchain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "StackChain — encode proven engineering decisions into automation",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md,yml,yaml}\"",
    "clean": "turbo run clean && rm -rf node_modules",
    "stackchain": "node packages/cli/dist/index.js",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "pack:check": "pnpm build && mkdir -p /tmp/stackchain-packs && pnpm --filter @stackchain/shared --filter @stackchain/engine --filter @stackchain/sdk --filter @stackchain/templates --filter @stackchain/registry --filter @stackchain/cli --filter stackchain exec pnpm pack --pack-destination /tmp/stackchain-packs",
    "publish:dry-run": "pnpm build && pnpm --filter \"./packages/*\" --filter \"./packages/stackchain\" publish --dry-run --access public --no-git-checks"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.11",
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.5",
    "@typescript-eslint/eslint-plugin": "^8.19.1",
    "@typescript-eslint/parser": "^8.19.1",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.19.1",
    "vitest": "^2.1.8"
  }
}
