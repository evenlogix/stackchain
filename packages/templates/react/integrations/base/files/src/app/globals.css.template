:root {
  color-scheme: light dark;
  --bg: #0b1220;
  --fg: #e8eef8;
  --accent: #3dd6c6;
}

* { box-sizing: border-box; }
html, body {
  margin: 0;
  min-height: 100%;
  background: radial-gradient(circle at top, #132033, var(--bg));
  color: var(--fg);
}
a { color: var(--accent); }
