# @evenlogix/stackchain-templates

Composable template layers for StackChain generators.

Shipped directories:

- `flutter/` — architectures, state-management, integrations
- `react/` — Next.js App Router layers (architecture, state, networking, auth)
- `backend/` — Node API layers (architecture, runtime, storage, auth)

MIT
