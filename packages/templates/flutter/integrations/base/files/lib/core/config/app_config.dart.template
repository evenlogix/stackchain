/// Application-level configuration.
///
/// Generated by StackChain. This project does not depend on StackChain at runtime.
class AppConfig {
  AppConfig._();

  static const String appName = '{{project_name}}';
  static const String packageId = '{{package_id}}';
  static const String organization = '{{organization}}';

  static Future<void> initialize() async {
    // Hook for async bootstrap (DI, feature flags, etc.).
  }
}
