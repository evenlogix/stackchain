import 'package:flutter/material.dart';
import 'package:{{project_name}}/controllers/home_controller.dart';

class HomeView extends StatelessWidget {
  HomeView({super.key, HomeController? controller})
      : _controller = controller ?? HomeController();

  final HomeController _controller;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text(_controller.title)),
      body: const Center(child: Text('MVC home view')),
    );
  }
}
